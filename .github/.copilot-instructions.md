# Instructions

This is a Deno 2.6.3 project solving Advent of Code 2025 challenges.

Tests can be run with `deno test -R`.
Single files can be tested with `deno test -R path/to/file.ts`.
Single test cases can be run with `deno test -R --filter "test case name"`.
